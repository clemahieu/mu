#pragma once

#include <vector>

namespace lambda_p
{
	namespace core
	{
		class association
		{
		public:
            std::vector < size_t> results;
			std::vector < size_t> parameters;
		};
	}
}

