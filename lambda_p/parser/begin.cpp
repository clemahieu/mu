#include <lambda_p/parser/begin.h>

lambda_p::parser::begin::begin(void)
{
}

lambda_p::parser::begin::~begin(void)
{
}

::lambda_p::parser::state_id lambda_p::parser::begin::state_type ()
{
	return lambda_p::parser::state_begin;
}
