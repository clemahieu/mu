#include "production_target.h"

#include <assert.h>
			
void lambda_p::parser::production_target::sink_reference (::lambda_p::parser::simple_parser & parser, ::lambda_p::parser::reference_identifiers reference)
{
	assert (false);
}

void lambda_p::parser::production_target::sink_data (::lambda_p::parser::simple_parser & parser, ::lambda_p::tokens::identifier * identifier)
{
	assert (false);
}

void lambda_p::parser::production_target::sink_declaration (::lambda_p::parser::simple_parser & parser, ::lambda_p::tokens::identifier * identifier)
{
	assert (false);
}