#pragma once

#include <lambda_p/node.h>

#include <vector>

#include <boost/shared_ptr.hpp>

namespace lambda_p
{
	// Class that can be referenced by `lambda_p::reference'
	class expression : public lambda_p::node
	{
	public:
	};
}

